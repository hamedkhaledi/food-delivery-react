class User {
  constructor(number, password, name, region, address, order) {
    this.number = number;
    this.password = password;
    this.name = name;
    this.region = region;
    this.address = address;
    this.order = order;
  }
}
export default User;
